from django.urls import path
from . import views

from django.urls import path
from . import views
#
urlpatterns = [
#     path('create/', views.TransactionCreateView.as_view(), name='create-transaction'),
#     path('list/', views.TransactionListView.as_view(), name='transaction-list'),
#     path('customer-transactions/', views.CustomerTransactionListView.as_view(), name='customer-transactions'),
#     path('account-transactions/<int:bank_account_id>/', views.BankAccountTransactionListView.as_view(), name='account-transactions'),  # Optional
]
# #